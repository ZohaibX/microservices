all docker images must be hosted to docker hub before we deploy the project to GCE 

may be we will be needed to to docker login to push that


// create a note, listing all the features of this project 

// add timing to jwt

// routes protection in next js 

// create admin user and show all accounts and have the ability to ban them 
// tell all the services, not to show anything to a banned user

// Persistent Volume Claim

// Routes Redirection

// add kubernetes deployment notes -- full fledged -- Digital Ocean 

// Add GCE

